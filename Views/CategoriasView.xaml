<UserControl x:Class="Enkarta.Views.CategoriasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1100">
	<Grid>
		<!-- VISTA DE TABLA -->
		<Grid x:Name="VistaTabla" Visibility="Visible">
			<Grid.RowDefinitions>
				<RowDefinition Height="Auto"/>
				<RowDefinition Height="*"/>
			</Grid.RowDefinitions>

			<!-- Barra de busqueda y acciones -->
			<Grid Grid.Row="0" Margin="0,0,0,20" x:Name="SearchPanel">
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*"/>
					<ColumnDefinition Width="Auto"/>
				</Grid.ColumnDefinitions>

				<!-- Buscador -->
				<StackPanel Grid.Column="0">
					<TextBlock Text="Buscar categoría por palabra clave"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TextBrush}"
                               Margin="0,0,0,8"
                               FontSize="13"/>
					<TextBox x:Name="txtBuscar"
                             Height="38"
                             VerticalContentAlignment="Center"
                             Background="{DynamicResource InputBrush}"
                             Foreground="{DynamicResource TextBrush}"
                             BorderBrush="{DynamicResource LineBrush}"
                             Padding="12,0"
                             FontSize="14"
                             TextChanged="TxtBuscar_TextChanged"/>
				</StackPanel>

				<!-- Boton Nueva Categoria -->
				<Button Grid.Column="1"
                        x:Name="btnNuevaCategoria"
                        Content="➕ Nueva Categoría"
                        Height="48"
                        Margin="20,0,0,0"
                        VerticalAlignment="Bottom"
                        Style="{StaticResource ButtonPrimary}"
                        Click="BtnNuevaCategoria_Click"/>
			</Grid>

			<!-- Tabla de Categorias -->
			<Border Grid.Row="1"
                    Background="{DynamicResource PanelBrush}"
                    BorderBrush="{DynamicResource LineBrush}"
                    BorderThickness="1"
                    CornerRadius="12"
                    Padding="0">
				<DataGrid x:Name="dgCategorias"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          Background="Transparent"
                          BorderThickness="0"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          RowHeight="52">
					<DataGrid.Columns>
						<DataGridTextColumn Header="ID"
                                            Binding="{Binding Id}"
                                            Width="80"/>
						<DataGridTextColumn Header="Nombre"
                                            Binding="{Binding Nombre}"
                                            Width="*"/>
						<DataGridTextColumn Header="Descripcion"
                                            Binding="{Binding Descripcion}"
                                            Width="2*"/>
						<DataGridTemplateColumn Header="Acciones" Width="80">
							<DataGridTemplateColumn.CellTemplate>
								<DataTemplate>
									<Button Content="✏️ Editar"
                                            Style="{StaticResource ButtonSoft}"
                                            Padding="4,2"
                                            FontSize="11"
                                            Height="22"
                                            Width="60"
                                            Click="BtnEditar_Click"
                                            Tag="{Binding}"/>
								</DataTemplate>
							</DataGridTemplateColumn.CellTemplate>
						</DataGridTemplateColumn>
					</DataGrid.Columns>
				</DataGrid>
			</Border>
		</Grid>

		<!-- VISTA DE FORMULARIO -->
		<Grid x:Name="VistaFormulario" Visibility="Collapsed">
			<Border Background="{DynamicResource PanelBrush}"
                    BorderBrush="{DynamicResource LineBrush}"
                    BorderThickness="1"
                    CornerRadius="12"
                    Padding="40"
                    MaxWidth="700"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    Margin="0,40,0,0">
				<StackPanel>
					<!-- Titulo del formulario -->
					<TextBlock x:Name="lblTituloFormulario"
                               Text="Nueva categoria"
                               Style="{StaticResource Heading1}"
                               Margin="0,0,0,30"/>

					<!-- Campo: Nombre -->
					<TextBlock Text="Nombre *"
                               FontWeight="SemiBold"
                               FontSize="14"
                               Foreground="{DynamicResource TextBrush}"
                               Margin="0,0,0,8"/>
					<TextBox x:Name="txtNombre"
                             Height="45"
                             Background="{DynamicResource InputBrush}"
                             Foreground="{DynamicResource TextBrush}"
                             BorderBrush="{DynamicResource LineBrush}"
                             Padding="15,0"
                             FontSize="14"
                             Margin="0,0,0,25"/>

					<!-- Campo: Descripcion -->
					<TextBlock Text="Descripcion"
                               FontWeight="SemiBold"
                               FontSize="14"
                               Foreground="{DynamicResource TextBrush}"
                               Margin="0,0,0,8"/>
					<TextBox x:Name="txtDescripcion"
                             Height="200"
                             Background="{DynamicResource InputBrush}"
                             Foreground="{DynamicResource TextBrush}"
                             BorderBrush="{DynamicResource LineBrush}"
                             Padding="15,12"
                             FontSize="14"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             Margin="0,0,0,30"/>

					<!-- Botones de accion -->
					<Grid>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="*"/>
							<ColumnDefinition Width="15"/>
							<ColumnDefinition Width="*"/>
						</Grid.ColumnDefinitions>

						<Button Grid.Column="0"
                                x:Name="btnCancelar"
                                Content="Cancelar"
                                Height="45"
                                Style="{StaticResource ButtonSoft}"
                                Click="BtnCancelar_Click"/>

						<Button Grid.Column="2"
                                x:Name="btnGuardar"
                                Content="Guardar"
                                Height="45"
                                Style="{StaticResource ButtonPrimary}"
                                Click="BtnGuardar_Click"/>
					</Grid>
				</StackPanel>
			</Border>
		</Grid>
	</Grid>
</UserControl>