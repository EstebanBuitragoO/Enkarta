<UserControl x:Class="Enkarta.Views.InicioView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="800" d:DesignWidth="1100">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!-- Contenido principal -->
        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Estadísticas rápidas -->
                <TextBlock Text="Estadísticas Rápidas"
                           Style="{StaticResource Heading2}"
                           Margin="0,0,0,20"/>

                <Grid Margin="0,0,0,30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Tarjeta: Artículos -->
                    <Border Grid.Column="0"
                            Background="{DynamicResource PanelBrush}"
                            BorderBrush="{DynamicResource LineBrush}"
                            BorderThickness="1"
                            CornerRadius="12"
                            Padding="24">
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Artículos"
                                       Foreground="{DynamicResource TextBrush}"
                                       FontSize="14"
                                       Margin="0,0,0,10"/>
                            <TextBlock x:Name="lblTotalArticulos"
                                       Text="0"
                                       Style="{StaticResource Heading1}"
                                       Foreground="{DynamicResource AccentBrush}"/>
                            <TextBlock Text="Total en el sistema"
                                       Style="{StaticResource TextMuted}"
                                       FontSize="12"
                                       Margin="0,8,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Tarjeta: Categorías -->
                    <Border Grid.Column="2"
                            Background="{DynamicResource PanelBrush}"
                            BorderBrush="{DynamicResource LineBrush}"
                            BorderThickness="1"
                            CornerRadius="12"
                            Padding="24">
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Categorías"
                                       Foreground="{DynamicResource TextBrush}"
                                       FontSize="14"
                                       Margin="0,0,0,10"/>
                            <TextBlock x:Name="lblTotalCategorias"
                                       Text="0"
                                       Style="{StaticResource Heading1}"
                                       Foreground="{DynamicResource AccentBrush}"/>
                            <TextBlock Text="Total en el sistema"
                                       Style="{StaticResource TextMuted}"
                                       FontSize="12"
                                       Margin="0,8,0,0"/>
                        </StackPanel>
                    </Border>

                    <!-- Tarjeta: Autores -->
                    <Border Grid.Column="4"
                            Background="{DynamicResource PanelBrush}"
                            BorderBrush="{DynamicResource LineBrush}"
                            BorderThickness="1"
                            CornerRadius="12"
                            Padding="24">
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Autores"
                                       Foreground="{DynamicResource TextBrush}"
                                       FontSize="14"
                                       Margin="0,0,0,10"/>
                            <TextBlock x:Name="lblTotalAutores"
                                       Text="0"
                                       Style="{StaticResource Heading1}"
                                       Foreground="{DynamicResource AccentBrush}"/>
                            <TextBlock Text="Total en el sistema"
                                       Style="{StaticResource TextMuted}"
                                       FontSize="12"
                                       Margin="0,8,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Acciones rápidas -->
                <TextBlock Text="Acciones Rápidas"
                           Style="{StaticResource Heading2}"
                           Margin="0,0,0,20"/>

                <Grid Margin="0,0,0,30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Botón: Nuevo Artículo -->
                    <Border Grid.Column="0"
                            Background="{DynamicResource PanelBrush}"
                            BorderBrush="{DynamicResource LineBrush}"
                            BorderThickness="1"
                            CornerRadius="12">
                        <Button x:Name="btnNuevoArticuloDesdeInicio"
                                Background="Transparent"
                                BorderThickness="0"
                                Padding="24"
                                Click="BtnNuevoArticuloDesdeInicio_Click"
                                Cursor="Hand">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="📝"
                                           FontSize="36"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,12"/>
                                <TextBlock Text="Nuevo Artículo"
                                           Foreground="{DynamicResource TextBrush}"
                                           FontWeight="SemiBold"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,8"/>
                                <TextBlock Text="Crear un nuevo artículo en el sistema"
                                           Style="{StaticResource TextMuted}"
                                           FontSize="12"
                                           TextAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </Border>

                    <!-- Botón: Nueva Categoría -->
                    <Border Grid.Column="2"
                            Background="{DynamicResource PanelBrush}"
                            BorderBrush="{DynamicResource LineBrush}"
                            BorderThickness="1"
                            CornerRadius="12">
                        <Button x:Name="btnNuevaCategoriasDesdeInicio"
                                Background="Transparent"
                                BorderThickness="0"
                                Padding="24"
                                Click="BtnNuevaCategoriasDesdeInicio_Click"
                                Cursor="Hand">
                            <StackPanel HorizontalAlignment="Center">
                                <TextBlock Text="🏷️"
                                           FontSize="36"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,12"/>
                                <TextBlock Text="Nueva Categoría"
                                           Foreground="{DynamicResource TextBrush}"
                                           FontWeight="SemiBold"
                                           HorizontalAlignment="Center"
                                           Margin="0,0,0,8"/>
                                <TextBlock Text="Crear una nueva categoría"
                                           Style="{StaticResource TextMuted}"
                                           FontSize="12"
                                           TextAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </Border>

                    <!-- Botón: Explorar Contenido -->
                    <Border Grid.Column="4"
                            Background="{DynamicResource PanelBrush}"
                            BorderBrush="{DynamicResource LineBrush}"
                            BorderThickness="1"
                            CornerRadius="12"
                            Padding="24">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="🔍"
                                       FontSize="36"
                                       HorizontalAlignment="Center"
                                       Margin="0,0,0,12"/>
                            <TextBlock Text="Explorar"
                                       Foreground="{DynamicResource TextBrush}"
                                       FontWeight="SemiBold"
                                       HorizontalAlignment="Center"
                                       Margin="0,0,0,8"/>
                            <TextBlock Text="Búsqueda avanzada de contenido"
                                       Style="{StaticResource TextMuted}"
                                       FontSize="12"
                                       TextAlignment="Center"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Últimos Artículos -->
                <TextBlock Text="Últimos Artículos"
                           Style="{StaticResource Heading2}"
                           Margin="0,0,0,20"/>

                <Border Background="{DynamicResource PanelBrush}"
                        BorderBrush="{DynamicResource LineBrush}"
                        BorderThickness="1"
                        CornerRadius="12"
                        Padding="1">
                    <DataGrid x:Name="dgUltimosArticulos"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              SelectionMode="Single"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              Background="Transparent"
                              BorderThickness="0"
                              SelectionChanged="DgUltimosArticulos_SelectionChanged"
                              GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column"
                              RowHeight="44">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Título"
                                                Binding="{Binding Titulo}"
                                                Width="*"/>
                            <DataGridTextColumn Header="Categoría"
                                                Binding="{Binding Categoria.Nombre}"
                                                Width="150"/>
                            <DataGridTextColumn Header="Fecha Publicación"
                                                Binding="{Binding FechaPublicacion, StringFormat='{}{0:dd/MM/yyyy}'}"
                                                Width="130"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>

            </StackPanel>
        </ScrollViewer>

    </Grid>
</UserControl>
